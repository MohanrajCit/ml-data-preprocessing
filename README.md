# 🧩 Task 1 — Data Preprocessing Project

This project performs **data cleaning, transformation, and normalization** using Python libraries like **Pandas**, **NumPy**, and **Scikit-learn**.  
It converts raw input data into a consistent, machine-learning-ready dataset.

---

## 🚀 Features
- Removes duplicate and invalid records  
- Handles missing values automatically  
- Encodes categorical features using `LabelEncoder`  
- Normalizes numeric columns between 0 and 1  
- Saves the processed dataset for further ML tasks  

---

## 🛠️ Technologies Used
- Python 3.x  
- Pandas  
- NumPy  
- Scikit-learn  


